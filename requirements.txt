# Runtime dependencies for the cms-ai plugin's LoopForge engine.
#
# These are PLUGIN-LOCAL deps (not core requirements). The OpenAI + Anthropic
# SDKs moved into CORE (S97) — the central LLM client owns them now — so cms-ai
# no longer pins them here (they remain importable because core provides them).
# Pillow + requests are already provided by core requirements.txt.
replicate==1.0.7
# The LoopForge flow files (loopforge-flow-*.yaml) are YAML; the asset loader
# reads them with PyYAML. Not a core dependency.
PyYAML==6.0.2
